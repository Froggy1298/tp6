@model List<Member>;
@{
    ViewData["Title"] = @Resource.ListChoixMembre;
}
<div class="d-flex justify-content-center flex-column align-items-center">
    <h1 class="p-2"><i class="fa fa-list"></i>@Resource.ListChoixMembre</h1>
    <table class="table-sm table-bordered table-striped"> 
        <thead>
             <tr>
              <th>@Html.DisplayNameFor(model => model[0].Name)</th>
              <th>@Html.DisplayNameFor(model => model[0].Email)</th>
              <th>@Html.DisplayNameFor(model => model[0].Username)</th>
              <th>@Html.DisplayNameFor(model => model[0].Role)</th>
              <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach(Member mb in Model)
            {
              <tr>
                  <td>@Html.DisplayFor(model => mb.Name)</td>
                  <td>@Html.DisplayFor(model => mb.Email)</td>
                  <td>@Html.DisplayFor(model => mb.Username)</td>
                  <td>@Html.DisplayFor(model => mb.Role)</td>
                  @*<td>@mn.Description</td>*@
                  <td class="text-center">
                    <a asp-route-id="@mb.Id" asp-action="Edit" data-toggle="tooltip" title="@Resource.Modifier"><i class="fa fa-pencil-square-o"></i></a> 
                    | 
                    <a asp-route-id="@mb.Id" asp-action="Delete" data-toggle="tooltip" title="@Resource.Supp"><i class="fa fa-trash-o"></i></a>
                  </td>
              </tr>  
            }
        </tbody>
    </table>
    <a asp-action="Create" class="btn-primary btn my-2">@Resource.AddMembre</a>
    <br/>
</div>
@section Scripts { 
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
     <script type="text/javascript">
            $(document).ready(function(){
              $('[data-toggle="tooltip"]').tooltip();
            });
      </script>
}